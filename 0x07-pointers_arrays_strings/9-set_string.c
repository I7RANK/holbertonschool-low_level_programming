#include "holberton.h"
#include <stdio.h>
/**
 * set_string - sets the value of a pointer to a char
 * Description: no description we
 * @s: is the pointer address
 * @to: is the pointer of the char
 * Return: no return
 */
void set_string(char **s, char *to)
{
	*s = to;
}
